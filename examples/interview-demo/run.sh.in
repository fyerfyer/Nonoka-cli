#!/usr/bin/env bash
set -euo pipefail

DEMO_ROOT="__DEMO_ROOT__"
if [ -f "$HOME/.config/nonoka/.env" ]; then
  set -a
  . "$HOME/.config/nonoka/.env"
  set +a
fi
export PATH="$DEMO_ROOT/.venv/bin:$PATH"
export NONOKA_EVENT_DB="$DEMO_ROOT/events.db"
cd "$DEMO_ROOT/project"
exec opencode
